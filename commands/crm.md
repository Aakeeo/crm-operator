---
description: Set up a new CRM (runs the business-onboarding interview) or open an existing one.
argument-hint: "[vault dir]"
---
Use the **crm-operator** skill's **Bootstrap** workflow.

Interview me about my business first — branding, what I sell, what I call my "deals"/"contacts", my pipeline stages (and what a win/loss is called), the 2–4 fields that matter most, and any custom objects I track. Propose a tailored `meta.profile`, play it back in plain English, and confirm before scaffolding.

Then scaffold and serve the vault, and offer to import my existing data.

Vault directory: $ARGUMENTS (if empty, ask me where to create it).
