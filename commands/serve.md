---
description: Start the local server for the CRM in this folder and give me the URL.
---
Use the **crm-operator** skill's **Serve** workflow: run `scripts/serve.mjs` in the background for the vault in this directory and give me the `http://127.0.0.1` URL. It auto-updates the vault's engine on start and is idempotent.
