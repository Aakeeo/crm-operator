{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/autoserve.sh\"",
            "statusMessage": "Bringing the CRM server up (if this folder is a vault)…"
          }
        ]
      }
    ]
  }
}
